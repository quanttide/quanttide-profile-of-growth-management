# Agent Instructions — 增长管理档案

增长管理领域各业务线的增长模型档案（AARRR）。**profile 是需要持续维护的活文档**——框架已定，工作方式是更新而非新建。

## 框架（固定，不得擅改）

```
data/profile/
├── README.md            # 索引
├── qtclass/             # 量潮课堂
│   ├── index.md         # 业务定位 + 关键判断 + 运营下放 + 度量
│   └── {acquisition,activation,retention,revenue,referral}.md   # AARRR 五阶段
├── qtdata/              # 量潮数据（同结构）
└── qtcloud/             # 量潮云（同结构）
```

三业务 × index + AARRR 五阶段。这个结构是既定框架，**新内容一律写进现有文件**。

## 工作纪律（最高优先级）

### 1. 在现有文件的基础上修改，禁止创建新文件

新判断、新策略、新动作、新指标——一律**更新对应的既有文档**：

- 业务级判断（如代金券定位、增长重点转移）→ 该业务 `index.md`
- 某个环节的新动作 → 对应阶段文档（acquisition/activation/…）
- 框架性认知 → README.md（仅限索引与全局原则）

判断"该进哪个文件"的方法：这段内容回答的是哪个环节的问题（获取/激活/留存/收入/推荐）？还是业务整体的定位与优先级（index）？**不因为内容是新主题就为新主题开新文件**——主题不是文件，是文件的章节。

### 2. 为什么禁止新文件

- 框架是既定的：三业务 × 五阶段是完整的分析骨架，任何增长问题都能落到既有格子里；新文件意味着框架没覆盖它，而正确的反应是检查是不是理解错了框架
- 文件数量失控 = 框架失效：profile 的价值在"每个业务一屏能看完增长全貌"，文件一多就退化成散乱的笔记堆
- 持续维护的状态假设：profile 不是一次性交付物，是长期滚动更新的状态文档——同一文件被反复修订（内容演进史在 git history 里），而不是不断另起炉灶

### 3. 修改方式

- **更新而非追加式堆砌**：新判断如果是对旧内容的修正，直接改写旧段落，不新旧并存（git history 保留演进）
- **保持文件形状**：每篇的结构（定位 → 现状 → 该做的事 → 度量）不因内容更新而变
- **减法优先**：过时的判断直接删除或改写，不保留"历史包袱"段落
- 内容确实装不下时，先向用户提方案（扩展现有文件 vs 框架本身要演进），用户拍板后再动

## 提交纪律

- 每次 commit 后自动 push；分层提交：本库 → quanttide-growth → 根仓库
- 子仓库操作前 `git checkout main && git pull`（常分离头，分离头提交用 ff-only 修复）
